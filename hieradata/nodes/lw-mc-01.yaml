
# Make this the master
classes:
    - munin::master
    - metacpan_postgres
    - metacpan_postgres::pgpass
    - minion_queue

minion_queue::service::workers: 17
minion_queue::service::ensure: running
minion_queue::service::enable: true

metacpan::web::starman:
  metacpan-web:
    git_revision: 'mi/es2'
  metacpan-api:
    git_revision: 'mi/es2'
