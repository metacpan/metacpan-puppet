# sample nginx server for search.metacpan.org

server {

    server_name  .<%= hostname %>.contest.metacpan.org .contest.metacpan.org;

    access_log  <%= nginx_log_dir %>/access.log;
    error_log   <%= nginx_log_dir %>/error.log;

    root  /home/metacpan/contest.metacpan.org;

    location / {
        index  index.html;
    }

    gzip               on;
    gzip_proxied       any;
    gzip_http_version  1.0;
    gzip_vary          on;
    gzip_buffers       16 8k;
    gzip_types         text/plain text/css text/javascript image/x-icon image/bmp application/xml;
    gzip_disable       "MSIE [1-6]\.";
}
