---
:backends:
  - yaml
:merge_behavior: deeper
:yaml:
  :datadir: /etc/puppet/hieradata
:hierarchy:
  - "nodes/%{hostname}"
  - "datacenter/%{datacenter}"
  - "env/%{env}"
  - private/common
  - common
